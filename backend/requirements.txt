# ---------------------------------------------------------------------------
# AI Slop Detector - backend requirements
# ---------------------------------------------------------------------------
# Install:  pip install -r requirements.txt
#           python -m spacy download en_core_web_sm
#
# Every model checkpoint is downloaded on first use from the Hugging Face Hub;
# none are vendored here. If the hub is unreachable the affected detectors
# disable themselves and say so - the service still starts.
# ---------------------------------------------------------------------------

# --- web service -----------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# --- numeric / classical ML ------------------------------------------------
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3

# --- deep learning ---------------------------------------------------------
# CPU-only install (much smaller, no CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
transformers>=4.40
sentence-transformers>=3.0
tokenizers>=0.19

# --- linguistics -----------------------------------------------------------
spacy>=3.7
# then:  python -m spacy download en_core_web_sm

# --- optional -------------------------------------------------------------
# Gradient-boosted meta-classifier and stylometry backends. The system runs
# without them; training/common.py exits with a clear message if you ask for an
# algorithm whose library is missing.
lightgbm>=4.0
# xgboost>=2.0

# Transformer fine-tuning (training/train_transformer.py only)
datasets>=2.18
accelerate>=0.29

# Optional cross-check for language identification. The bundled stop-word
# profile classifier works without it.
# langdetect>=1.0.9

# --- development / test ----------------------------------------------------
pytest>=8.0
httpx>=0.27
